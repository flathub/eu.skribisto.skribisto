app-id: eu.skribisto.skribisto
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: skribisto
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=home
  - --device=dri
  - --own-name=com.druide.antidote.*
  - --talk-name=org.freedesktop.Flatpak
separate-locales: false
modules:
  - name: skribisto
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=RelWithDebInfo
    sources:
      - type: git
        url: https://github.com/jacquetc/skribisto.git
        tag: v1.9.43
        commit: c2f44b0c85a30dbf8a461de57a842f3dc56a3618
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)
