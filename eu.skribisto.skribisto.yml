app-id: eu.skribisto.skribisto
runtime: org.kde.Platform
runtime-version: '6.2'
sdk: org.kde.Sdk
command: skribisto
finish-args:
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --filesystem=home
  - --device=dri
  - --own-name=com.druide.antidote.*
  - --talk-name=org.freedesktop.Flatpak
separate-locales: false
modules:
        
  - name: skribisto
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=RelWithDebInfo
    sources:
      - type: git
        url: https://github.com/jacquetc/skribisto.git
        tag: v1.9.35
        commit: 0a2951e2888d6809920ce3ea740612041dd89cfa
