app-id: eu.skribisto.skribisto
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: skribisto
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=home
  - --device=dri
  - --own-name=com.druide.antidote.*
  - --talk-name=org.freedesktop.Flatpak
separate-locales: false
modules:
  - name: skribisto
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=RelWithDebInfo
    sources:
      - type: git
        url: https://github.com/jacquetc/skribisto.git
        tag: v1.9.43
        commit: d7bb6cf5d566ee2c723ef9e2da3a0f0568f8f141
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)
